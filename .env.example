APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=credverify
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS Configuration
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Blockchain Configuration
BLOCKCHAIN_NETWORK=sepolia
ETHEREUM_RPC_URL=https://sepolia.infura.io/v3/
INFURA_PROJECT_ID=
ALCHEMY_API_KEY=

# Contract Addresses (Ethereum)
SEPOLIA_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
GOERLI_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
MAINNET_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# Wallet Configuration (Keep these secure!)
BLOCKCHAIN_WALLET_ADDRESS=0x0000000000000000000000000000000000000000
BLOCKCHAIN_PRIVATE_KEY=
BLOCKCHAIN_PUBLIC_KEY=

# Transaction Settings
BLOCKCHAIN_GAS_LIMIT=300000
BLOCKCHAIN_GAS_PRICE=20000000000
BLOCKCHAIN_MAX_PRIORITY_FEE=2000000000
BLOCKCHAIN_MAX_FEE=50000000000
BLOCKCHAIN_BATCH_SIZE=10
BLOCKCHAIN_CONFIRMATION_BLOCKS=12
BLOCKCHAIN_MAX_RETRIES=3
BLOCKCHAIN_RETRY_DELAY=15

# Blockcerts Configuration
BLOCKCERTS_ISSUER_NAME="${APP_NAME}"
BLOCKCERTS_ISSUER_URL="${APP_URL}"
BLOCKCERTS_ISSUER_EMAIL="issuer@example.com"
BLOCKCERTS_ISSUER_IMAGE="${APP_URL}/images/logo.png"
BLOCKCERTS_ISSUER_PUBLIC_KEY=""
BLOCKCERTS_ISSUER_REVOCATION_LIST="${APP_URL}/api/revocations/"
BLOCKCERTS_ISSUER_SIGNATURE_SUITE="Ed25519Signature2020"

# File Storage (local, public, s3, etc.)
FILESYSTEM_DISK=public

# Session Configuration (for production use database or redis)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=false

# Cache Configuration (for production use redis or memcached)
CACHE_DRIVER=file

# Queue Configuration (for production use redis or database)
QUEUE_CONNECTION=sync

# Redis Configuration
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
